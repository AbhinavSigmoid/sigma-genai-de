name: 'sigma_dbt'
version: '1.0.0'
config-version: 2

profile: 'sigma_dbt'

model-paths: ["models"]

models:
  sigma_dbt:
    staging:
      +materialized: ephemeral
    marts:
      +materialized: ephemeral