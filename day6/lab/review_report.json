{
  "reviewed_at": "2026-05-25T12:19:33.878600",
  "model": "amazon.nova-lite-v1:0",
  "queries": {
    "revenue_by_merchant": {
      "sql": "SELECT m.merchant_name,\n       SUM(t.amount) as total_revenue,\n       COUNT(*) as txn_count\nFROM fact_transactions t, dim_merchant m\nWHERE t.merchant_id = m.merchant_id\nAND t.transaction_date > '2024-01-01'\nGROUP BY m.merchant_name\nORDER BY total_revenue\nLIMIT 10;",
      "issues_found": 3,
      "review": {
        "issues": [
          {
            "severity": "Critical",
            "category": "Correctness",
            "title": "Missing filter for completed transactions",
            "line": "SELECT clause",
            "problem": "The query does not filter transactions by status to only include completed transactions.",
            "fix": "Add AND t.status = 'COMPLETED' to the WHERE clause."
          },
          {
            "severity": "Medium",
            "category": "Performance",
            "title": "Implicit join syntax",
            "line": "FROM clause",
            "problem": "Using implicit join syntax can lead to less readable and maintainable code.",
            "fix": "Replace the comma-separated tables with an explicit JOIN clause."
          },
          {
            "severity": "Medium",
            "category": "Readability",
            "title": "Lack of comments",
            "line": "Entire query",
            "problem": "The query lacks comments explaining the logic and purpose.",
            "fix": "Add comments to explain the purpose of each clause and filter."
          }
        ],
        "corrected_sql": "SELECT m.merchant_name,\n       SUM(t.amount) as total_revenue,\n       COUNT(*) as txn_count\nFROM fact_transactions t\nJOIN dim_merchant m ON t.merchant_id = m.merchant_id\nWHERE t.transaction_date > '2024-01-01'\nAND t.status = 'COMPLETED'\nGROUP BY m.merchant_name\nORDER BY total_revenue DESC\nLIMIT 10;",
        "summary": "The query has a critical correctness issue by not filtering for completed transactions. It also uses implicit join syntax and lacks comments, impacting readability."
      }
    },
    "customer_spend": {
      "sql": "SELECT c.customer_name, c.email, c.tier,\n       SUM(t.amount) as lifetime_value,\n       COUNT(t.transaction_id) as total_orders\nFROM dim_customer c\nLEFT JOIN fact_transactions t ON c.customer_id = t.customer_id\nWHERE t.status = 'COMPLETED'\nGROUP BY c.customer_name, c.email\nHAVING SUM(t.amount) > 1000\nORDER BY lifetime_value DESC;",
      "issues_found": 4,
      "review": {
        "issues": [
          {
            "severity": "Critical",
            "category": "Correctness",
            "title": "Incorrect lifetime_value calculation",
            "line": "SUM(t.amount) as lifetime_value",
            "problem": "The lifetime_value is calculated as the sum of all transaction amounts, not just completed ones.",
            "fix": "SUM(CASE WHEN t.status = 'COMPLETED' THEN t.amount ELSE 0 END) as lifetime_value"
          },
          {
            "severity": "Medium",
            "category": "Correctness",
            "title": "Missing filter for 'COMPLETED' status",
            "line": "WHERE t.status = 'COMPLETED'",
            "problem": "The WHERE clause is filtering on the fact_transactions table, but it should be in the JOIN condition to ensure all customers are included.",
            "fix": "LEFT JOIN fact_transactions t ON c.customer_id = t.customer_id AND t.status = 'COMPLETED'"
          },
          {
            "severity": "Medium",
            "category": "Readability",
            "title": "Lack of comments",
            "line": "Entire query",
            "problem": "The query lacks comments explaining the purpose and logic of the query.",
            "fix": "Add comments explaining the purpose of each part of the query."
          },
          {
            "severity": "Low",
            "category": "Readability",
            "title": "Inconsistent naming",
            "line": "SUM(t.amount) as lifetime_value",
            "problem": "The alias 'lifetime_value' could be more descriptive, such as 'total_completed_value'.",
            "fix": "SUM(CASE WHEN t.status = 'COMPLETED' THEN t.amount ELSE 0 END) as total_completed_value"
          }
        ],
        "corrected_sql": "SELECT c.customer_name, c.email, c.tier, SUM(CASE WHEN t.status = 'COMPLETED' THEN t.amount ELSE 0 END) as total_completed_value, COUNT(t.transaction_id) as total_orders FROM dim_customer c LEFT JOIN fact_transactions t ON c.customer_id = t.customer_id AND t.status = 'COMPLETED' GROUP BY c.customer_name, c.email HAVING SUM(CASE WHEN t.status = 'COMPLETED' THEN t.amount ELSE 0 END) > 1000 ORDER BY total_completed_value DESC;",
        "summary": "The query had issues with correctness in calculating lifetime value and filtering on transaction status. Readability was also improved by adding comments and renaming aliases."
      }
    },
    "daily_failure_rate": {
      "sql": "SELECT transaction_date,\n       (SELECT COUNT(*) FROM fact_transactions f2\n        WHERE f2.status = 'FAILED'\n        AND f2.transaction_date = t.transaction_date) as failed_count,\n       COUNT(*) as total_count,\n       failed_count / total_count * 100 as failure_rate\nFROM fact_transactions t\nGROUP BY transaction_date\nORDER BY transaction_date;",
      "issues_found": 3,
      "review": {
        "issues": [
          {
            "severity": "Critical",
            "category": "Correctness",
            "title": "Incorrect calculation of failure rate",
            "line": "failed_count / total_count * 100 as failure_rate",
            "problem": "The failure rate is being calculated incorrectly because failed_count is not defined in the SELECT clause but used in the calculation.",
            "fix": "failed_count / total_count * 100 as failure_rate"
          },
          {
            "severity": "Medium",
            "category": "Performance",
            "title": "Correlated subquery",
            "line": "SELECT COUNT(*) FROM fact_transactions f2 WHERE f2.status = 'FAILED' AND f2.transaction_date = t.transaction_date",
            "problem": "The correlated subquery can be inefficient as it runs for each row in the outer query.",
            "fix": "Use a JOIN with a pre-aggregated subquery."
          },
          {
            "severity": "Medium",
            "category": "Readability",
            "title": "Lack of comments",
            "line": "Entire query",
            "problem": "The query lacks comments explaining the logic, especially around the failure rate calculation.",
            "fix": "Add comments to explain the logic and calculations."
          }
        ],
        "corrected_sql": "WITH failed_transactions AS (SELECT transaction_date, COUNT(*) AS failed_count FROM fact_transactions WHERE status = 'FAILED' GROUP BY transaction_date) SELECT t.transaction_date, COALESCE(f.failed_count, 0) AS failed_count, COUNT(*) AS total_count, COALESCE(f.failed_count, 0) * 100.0 / COUNT(*) AS failure_rate FROM fact_transactions t LEFT JOIN failed_transactions f ON t.transaction_date = f.transaction_date GROUP BY t.transaction_date ORDER BY t.transaction_date;",
        "summary": "The query had a critical correctness issue with the failure rate calculation and a performance issue with the correlated subquery. Readability was also improved by adding comments."
      }
    }
  }
}